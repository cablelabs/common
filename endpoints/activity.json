{
  "/notifications": {
    "get": {
      "tags": [
        "Local Access and Transport Area (Notification)"
      ], 
      "summary": "Fetch all Notifications that meet the query criteria.", 
      "description": "This service fetches all Notifications that meet the query criteria.", 
      "displayName": "Notification", 
      "produces": [
        "application/json", 
        "application/xml"
      ],
      "parameters": [
        {
          "in": "query",
          "name": "filter",
          "description": "Filter defining fields, where, include, order, offset, and limit.",
          "required": false,
          "type": "string"
        }
      ],
      "responses": {
        "200": {
          "description": "Successful operation.",
          "schema": {
            "$ref": "#/definitions/notificationList"
          }
        }
      }
    },
    "post": {
      "tags": [
        "Notification"
      ], 
      "summary": "Create a new Notification.", 
      "description": "This service creates a new Notification.", 
      "consumes": [
        "application/json", 
        "application/xml"
      ], 
      "produces": [
        "application/json", 
        "application/xml"
      ],
      "parameters": [
        {
          "in": "body",
          "name": "data",
          "description": "Resource instance data.",
          "required": true,
          "schema": {
            "$ref": "#/definitions/notification"
          }
        }
      ],
      "responses": {
        "200": {
          "description": "Successful operation.",
          "schema": {
            "$ref": "#/definitions/notificationList"
          }
        },
        "201": {
          "description": "Instance created."
        }, 
        "400": {
          "description": "Bad Request."
        }
      }
    }
  }
}
